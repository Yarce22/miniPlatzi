import {PlatziClass} from "./main.mjs";

const clase67 = new PlatziClass ({
    name: 'JavaScript: oriendtado a objetos, basado en prototipos',
    videoID: '1'
});

clase67.reproducir();
clase67.pausar();

clase67.reproducir();
clase67.pausar();

clase67.reproducir();
clase67.pausar();

const hola = 2

function holaString(string) {
    
    if (typeof(string) == 'string') {
        console.log('True'); 
    } else {
        console.log('False')
    }
};

holaString(hola);